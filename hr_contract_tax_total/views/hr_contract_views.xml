<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="hr_contract_view_form" model="ir.ui.view">
        <field name="name">hr.contract.view.form (in hr_contract_tax_total)</field>
        <field name="model">hr.contract</field>
        <field name="inherit_id" ref="hr_contract.hr_contract_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//group[@name='salary']" position="after">
                <group name="taxes">
                    <label for="tax_total_pct"/>
                    <div class="o_row" name="tax_total_pct">
                        <field name="tax_total_pct" widget="percentage" required="1"/>
                    </div>
                </group>
            </xpath>
        </field>
    </record>

    <record id="hr_contract_view_tree" model="ir.ui.view">
        <field name="name">hr.contract.view.tree (in hr_contract_tax_total)</field>
        <field name="model">hr.contract</field>
        <field name="inherit_id" ref="hr_contract.hr_contract_view_tree"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='wage']" position="after">
                <field name="tax_total_pct" optional="hide" widget="percentage"/>
            </xpath>
        </field>
    </record>
</odoo>
