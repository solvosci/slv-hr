If a menu “Expense product configurations" is added in the future, add the following rules:
* Regular employees can only see their own expense configuration.
* Expense approvers can see all configurations
